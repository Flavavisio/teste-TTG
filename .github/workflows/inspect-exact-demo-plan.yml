name: Inspect app migration boundary

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/separar-index-app
      - name: Estado dos módulos principais
        run: |
          python3 - <<'PY'
          from pathlib import Path
          text = Path('app.html').read_text(encoding='utf-8')
          checks = [
              'window.TotalGestApp.init',
              'function abrirModal(',
              'function _salvarFormularioInterno(',
              'window.TotalGestSaveFormFornecedor.run({',
              'window.TotalGestSaveFormArtigo.run({',
              'window.TotalGestSaveFormRequisicao.run({',
              'window.TotalGestSaveFormObra.run({',
              'window.TotalGestSaveFormFolha.run({',
              'window.TotalGestSaveFormCliente.prepare({',
              'window.TotalGestSaveFormFuncionarioOrchestrator.run({',
              'window.TotalGestSaveFormServico.run({',
              'window.TotalGestSaveFormPersist.apply({',
              'window.TotalGestSaveFormServicoNotifications.run({',
              'window.TotalGestSaveFormFolhaUsage.apply({',
              'window.TotalGestSaveFormFolhaOsPending.apply({',
              'window.TotalGestSaveFormFolhaObraPending.apply({',
              'window.TotalGestSaveFormFinalize.run({',
              'window.TotalGestSaveFormAuth.run({',
              'function guardarDados()',
              'async function _sincronizar()',
              'function bootstrapSupabase()',
              'async function carregarDados('
          ]
          for item in checks:
              print(item, text.count(item))
          PY
