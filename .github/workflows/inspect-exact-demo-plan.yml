name: Inspect folha modal block

on:
  push:
    branches:
      - refactor/separar-index-app
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inspect:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/separar-index-app
      - name: Inspecionar folha
        run: |
          python3 - <<'PY'
          from pathlib import Path
          text=Path('app.html').read_text(encoding='utf-8')
          m=text.index('        function abrirModal(')
          a="            } else if (entidade === 'folha') {\n"; b="            } else if (entidade === 'requisicao') {\n"
          s=text.index(a,m); e=text.index(b,s); print(text[s:e])
          PY
