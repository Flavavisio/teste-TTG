name: Inspect app migration boundary

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/separar-index-app
      - name: Estado dos módulos de serviços
        run: |
          python3 - <<'PY'
          from pathlib import Path
          text=Path('app.html').read_text(encoding='utf-8')
          marker='        function renderizarServicos() {'
          s=text.index(marker); e=text.index('\n        function ',s+len(marker)); block=text[s:e]
          for item in ['createServicesRuntimeState({','createServicesAreaRendererFromDocument({','renderDependencies: runtimeState.renderDependencies','renderServicesArea(runtimeState.selectionState)','buildActions: (s, rowData) => ({']:
              assert block.count(item)==1,(item,block.count(item))
          for item in ['servicesViewElements(document)','prepareServicesForRendering({','createServiceRenderDependencies({','createServicesAreaRenderer({','selectPending: renderDependencies.selectPendingSpecialty','prepareRow: renderDependencies.prepareRow','const servicos = selectionState.services;','createPendingSpecialtySelector({','renderPendingSpecialtyNotice({','createServicesTableStateRenderer({','renderTableState(','createServiceRowPreparerFromData({','createServiceRowsRenderer({','renderPreparedRows(']:
              assert block.count(item)==0,(item,block.count(item))
          for item in ['invoiceTOId:','invoiceMoloniId:','invoiceMoloniUrl:','receiptMoloniUrl:','guideMoloniId:','guideMoloniUrl:','creditNoteMoloniId:','creditNoteMoloniUrl:']:
              assert block.count(item)==1,(item,block.count(item))
          assert "const CACHE = 'totalgest-v150';" in Path('sw.js').read_text(encoding='utf-8')
          print('APP_MIGRATION_BOUNDARY=OK')
          PY
