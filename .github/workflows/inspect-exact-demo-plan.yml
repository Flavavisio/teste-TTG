name: Migrate load transform

on:
  push:
    branches:
      - refactor/separar-index-app
  workflow_dispatch:

permissions:
  contents: write

jobs:
  migrate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/separar-index-app
      - name: Extrair transformação do carregamento
        run: python3 .github/scripts/migrate-load-transform.py
      - name: Validar JavaScript
        run: |
          node --check assets/js/app-load-transform.js
          node --check assets/js/app-shell.js
          node --check sw.js
      - name: Validar e gravar
        run: |
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm .github/scripts/migrate-load-transform.py
          git add app.html assets/js/app-load-transform.js assets/js/app-shell.js sw.js .github/scripts/migrate-load-transform.py
          git diff --cached --check
          git commit -m "Extrair transformação do carregamento inicial"
          git push origin HEAD:refactor/separar-index-app
